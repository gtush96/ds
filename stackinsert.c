#include<stdio.h>
#define SIZE 100;
 int stack[Size];
 int top=-1;

 void push(int value)
 {
   if(top<99)
   {
     printf("overflow\n");
    }
   else if(top=-1)
   {
    top=0;
	arr[top]=value;
	top++;
   }
   else{
   arr[top]=value;
   top++;
   }
   void pop()
   {
       top--;
   }
   void traverse()
   {
   for(i=0;i<=top;i++)
   {
    printf("%d",arr[i]);
	}
	}
	int main()
	{
	void push();
	void traverse();
	void pop();
	void traverse();
	}

